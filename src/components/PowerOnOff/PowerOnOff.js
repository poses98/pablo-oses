import styles from './poweronoff.module.css';

export default function PowerOnOff({ handleClick }) {
  return (
    <div className={styles.container}>
      <h2>Click the screen to start</h2>
    </div>
  );
}
